<<<<<<< HEAD
@using foodbook.Helpers
@{ var currentController = ViewContext.RouteData.Values["Controller"] as string; var currentAction = ViewContext.RouteData.Values["Action"] as string; }
<div class="card sidebar-card sticky-sidebar">
    <div class="card-body">
        @if (Context.Session.IsAdmin())
        {
            <div class="mb-4">
                <div class="sidebar-section-title">ADMIN PANEL</div>
                <ul class="sidebar-menu mb-0">
                    <li class="sidebar-item @(string.Equals(currentController, "Admin", StringComparison.OrdinalIgnoreCase) ? "active" : "")">
                        <a class="sidebar-link" asp-controller="Admin" asp-action="Dashboard">
                            <span class="sidebar-icon"><i class="bi bi-house"></i></span> Tổng quan hệ thống
                        </a>
                    </li>
                    <li class="sidebar-item @(string.Equals(currentController, "Admin", StringComparison.OrdinalIgnoreCase) && string.Equals(currentAction, "Users", StringComparison.OrdinalIgnoreCase) ? "active" : "")">
                        <a class="sidebar-link" asp-controller="Admin" asp-action="Dashboard">
                            <span class="sidebar-icon"><i class="bi bi-person-square"></i></span> Quản lý người dùng
                        </a>
                    </li>
                    <li class="sidebar-item @(string.Equals(currentController, "Admin", StringComparison.OrdinalIgnoreCase) && string.Equals(currentAction, "Moderation", StringComparison.OrdinalIgnoreCase) ? "active" : "")">
                        <a class="sidebar-link" asp-controller="Admin" asp-action="Dashboard">
                            <span class="sidebar-icon"><i class="bi bi-shield-check"></i></span> Kiểm duyệt nội dung
                        </a>
                    </li>
                    <li class="sidebar-item @(string.Equals(currentController, "Admin", StringComparison.OrdinalIgnoreCase) && string.Equals(currentAction, "Categories", StringComparison.OrdinalIgnoreCase) ? "active" : "")">
                        <a class="sidebar-link" asp-controller="Admin" asp-action="Dashboard">
                            <span class="sidebar-icon"><i class="bi bi-funnel"></i></span> Quản lý phân loại
                        </a>
                    </li>
                    <li class="sidebar-item @(string.Equals(currentController, "Admin", StringComparison.OrdinalIgnoreCase) && string.Equals(currentAction, "Ingredients", StringComparison.OrdinalIgnoreCase) ? "active" : "")">
                        <a class="sidebar-link" asp-controller="Admin" asp-action="Dashboard">
                            <span class="sidebar-icon"><i class="bi bi-basket"></i></span> Quản lý nguyên liệu
                        </a>
                    </li>
                </ul>
            </div>
        }

        <div class="sidebar-section-title">TÀI KHOẢN CỦA TÔI</div>
        <ul class="sidebar-menu mb-0">
            <li class="sidebar-item @(string.Equals(currentController, "Account", StringComparison.OrdinalIgnoreCase) && string.Equals(currentAction, "Profile", StringComparison.OrdinalIgnoreCase) ? "active" : "")">
                <a class="sidebar-link" asp-controller="Account" asp-action="Profile">
                    <span class="sidebar-icon"><i class="bi bi-person"></i></span> Hồ sơ của tôi
                </a>
            </li>
            <li class="sidebar-item @(string.Equals(currentController, "Notebook", StringComparison.OrdinalIgnoreCase) ? "active" : "")">
                <a class="sidebar-link" href="#">
                    <span class="sidebar-icon"><i class="bi bi-bookmark"></i></span> Sổ tay của tôi
                </a>
            </li>
            <li class="sidebar-item @(string.Equals(currentController, "Recipe", StringComparison.OrdinalIgnoreCase) && string.Equals(currentAction, "Create", StringComparison.OrdinalIgnoreCase) ? "active" : "")">
                <a class="sidebar-link" asp-controller="Recipe" asp-action="Create">
                    <span class="sidebar-icon"><i class="bi bi-plus-lg"></i></span> Thêm công thức mới
                </a>
            </li>
            <li class="sidebar-item @(string.Equals(currentController, "Settings", StringComparison.OrdinalIgnoreCase) ? "active" : "")">
                <a class="sidebar-link" href="#">
                    <span class="sidebar-icon"><i class="bi bi-gear"></i></span> Cài đặt
                </a>
            </li>
        </ul>
    </div>
</div>

=======
﻿@using foodbook.Helpers

<div class="sidebar">
    <div class="sidebar-header">
        <h5 class="text-uppercase fw-bold mb-3">TÀI KHOẢN CỦA TÔI</h5>
    </div>

    <nav class="sidebar-nav">
        <ul class="nav flex-column">
            <li class="nav-item">
                <a class="nav-link d-flex align-items-center text-gray" asp-controller="Account" asp-action="Profile">
                    <i class="bi bi-person me-2"></i>
                    <span>Hồ sơ của tôi</span>
                </a>
            </li>

            <li class="nav-item">
                <a class="nav-link d-flex align-items-center text-gray" asp-controller="Recipe" asp-action="MyRecipes">
                    <i class="bi bi-journal-text me-2"></i>
                    <span>Sổ tay của tôi</span>
                </a>
            </li>

            <li class="nav-item">
                <a class="nav-link d-flex align-items-center text-gray" asp-controller="Recipe" asp-action="AddRecipe">
                    <i class="bi bi-plus-circle me-2"></i>
                    <span>Thêm công thức mới</span>
                </a>
            </li>

            <li class="nav-item">
                <a class="nav-link d-flex align-items-center text-gray" asp-controller="Account" asp-action="Settings">
                    <i class="bi bi-gear me-2"></i>
                    <span>Cài đặt</span>
                </a>
            </li>
        </ul>
    </nav>
</div>

<script>
    // Highlight active menu item based on current URL
    document.addEventListener('DOMContentLoaded', function() {
        const currentPath = window.location.pathname.toLowerCase();
        const navLinks = document.querySelectorAll('.sidebar-nav .nav-link');

        navLinks.forEach(link => {
            const linkPath = link.getAttribute('href')?.toLowerCase();
            if (linkPath && currentPath.includes(linkPath)) {
                link.classList.add('active');
            }
        });
    });
</script>
>>>>>>> 02daccfdfe5df7d2acf59dce1834f789d1f9b788
