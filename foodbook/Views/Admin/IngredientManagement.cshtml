@model List<foodbook.Models.IngredientMaster>
@{
    ViewData["Title"] = "Quản lý nguyên liệu";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
.ingredient-page {
    padding: 2rem;
    max-width: 1400px;
    margin: 0 auto;
}

.ingredient-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 2rem;
}

.ingredient-title {
    font-size: 2rem;
    font-weight: 700;
    color: #212529;
}

.search-box {
    position: relative;
    flex: 1;
    max-width: 400px;
    margin-bottom: 1.5rem;
}

.search-box i {
    position: absolute;
    left: 1rem;
    top: 50%;
    transform: translateY(-50%);
    color: #6c757d;
}

.search-input {
    width: 100%;
    padding: 0.75rem 1rem 0.75rem 2.5rem;
    border: 1px solid #dee2e6;
    border-radius: 8px;
    font-size: 0.95rem;
    transition: all 0.2s;
}

.search-input:focus {
    outline: none;
    border-color: #1dd75b;
    box-shadow: 0 0 0 3px rgba(29, 215, 91, 0.1);
}

.table-container {
    background: white;
    border-radius: 12px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
    overflow: hidden;
}

.ingredient-table {
    width: 100%;
    border-collapse: collapse;
}

.ingredient-table thead {
    background: #f8f9fa;
}

.ingredient-table th {
    padding: 1rem 1.5rem;
    text-align: left;
    font-weight: 600;
    color: #495057;
    border-bottom: 2px solid #dee2e6;
    font-size: 0.95rem;
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

.ingredient-table tbody tr {
    border-bottom: 1px solid #f1f3f5;
    transition: all 0.2s;
}

.ingredient-table tbody tr:hover {
    background: #f8f9fa;
}

.ingredient-table td {
    padding: 1rem 1.5rem;
    color: #495057;
    font-size: 0.95rem;
}

.btn-add {
    background: #1dd75b;
    color: white;
    border: none;
    padding: 0.75rem 1.5rem;
    border-radius: 8px;
    font-size: 0.95rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s;
}

.btn-add:hover {
    background: #18b84d;
    transform: translateY(-2px);
}

.btn-delete {
    background: #dc3545;
    color: white;
    border: none;
    padding: 0.5rem 0.75rem;
    border-radius: 6px;
    cursor: pointer;
    transition: all 0.2s;
}

.btn-delete:hover {
    background: #c82333;
    transform: scale(1.05);
}
</style>

<div class="ingredient-page">
    <div class="ingredient-header">
        <h1 class="ingredient-title">Quản lý nguyên liệu</h1>
        <button type="button" class="btn-add" data-bs-toggle="modal" data-bs-target="#addModal">
            <i class="fas fa-plus"></i> Thêm nguyên liệu
        </button>
    </div>

    <div class="search-box">
        <i class="fas fa-search"></i>
        <input type="text" class="search-input" placeholder="Tìm kiếm nguyên liệu..." id="searchInput">
    </div>

    <div class="table-container">
        <table class="ingredient-table" id="ingredientTable">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Tên nguyên liệu</th>
                    <th>Ngày tạo</th>
                    <th>Hành động</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null && Model.Any())
                {
                    foreach (var ingredient in Model)
                    {
                        <tr data-name="@ingredient.name?.ToLower()">
                            <td>@ingredient.ingredient_id</td>
                            <td>@ingredient.name</td>
                            <td>@ingredient.created_at.ToString("yyyy-MM-dd")</td>
                            <td>
                                <button type="button" class="btn-delete" 
                                        onclick="showDeleteModal(@ingredient.ingredient_id, '@ingredient.name')"
                                        title="Xóa">
                                    <i class="fas fa-trash"></i>
                                </button>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="4" class="text-center">Không có nguyên liệu nào</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<div class="modal fade" id="addModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Thêm nguyên liệu mới</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <form method="post" action="@Url.Action("AddIngredient", "Admin")">
                <div class="modal-body">
                    <div class="mb-3">
                        <label for="ingredientName" class="form-label">Tên nguyên liệu</label>
                        <input type="text" class="form-control" id="ingredientName" name="name" required>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Hủy</button>
                    <button type="submit" class="btn btn-primary">Thêm</button>
                </div>
            </form>
        </div>
    </div>
</div>

<div class="modal fade" id="deleteModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Xác nhận xóa</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                Bạn có chắc chắn muốn xóa nguyên liệu <strong id="deleteIngredientName"></strong>?
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Hủy</button>
                <form method="post" action="@Url.Action("DeleteIngredient", "Admin")" id="deleteForm">
                    <input type="hidden" name="ingredientId" id="deleteIngredientId" />
                    <button type="submit" class="btn btn-danger">Xóa</button>
                </form>
            </div>
        </div>
    </div>
</div>

<script>
function showDeleteModal(ingredientId, ingredientName) {
    document.getElementById('deleteIngredientId').value = ingredientId;
    document.getElementById('deleteIngredientName').textContent = ingredientName;
    new bootstrap.Modal(document.getElementById('deleteModal')).show();
}

document.addEventListener('DOMContentLoaded', function() {
    const searchInput = document.getElementById('searchInput');
    const tbody = document.querySelector('#ingredientTable tbody');

    searchInput.addEventListener('input', function() {
        const searchTerm = this.value.toLowerCase();
        const rows = tbody.querySelectorAll('tr');
        
        rows.forEach(row => {
            const name = row.dataset.name || '';
            
            if (name.includes(searchTerm)) {
                row.style.display = '';
            } else {
                row.style.display = 'none';
            }
        });
    });
});
</script>
