@{
    ViewData["Title"] = "Xác thực thành công";
}

<div class="container d-flex justify-content-center align-items-center" style="min-height: 80vh;">
    <div class="card shadow-sm verify-success-card">
        <div class="card-body p-5 text-center">
            <div class="mb-4">
                <i class="bi bi-check-circle-fill text-success" style="font-size: 4rem;"></i>
            </div>
            
            <h2 class="text-success mb-3 fw-bold">🎉 Xác thực thành công!</h2>
            
            <p class="text-muted mb-4 fs-5">
                Tài khoản của bạn đã được xác thực thành công.<br>
                Chào mừng bạn đến với Foodbook!
            </p>

            @if (TempData["SuccessMessage"] != null)
            {
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    <i class="bi bi-check-circle me-2"></i>
                    @TempData["SuccessMessage"]
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            }

            <div class="mt-4">
                <a href="/Home" class="btn btn-success btn-lg me-3">
                    <i class="bi bi-house me-2"></i>
                    Về trang chủ
                </a>
                <a href="/Account/Login" class="btn btn-outline-success btn-lg">
                    <i class="bi bi-box-arrow-in-right me-2"></i>
                    Đăng nhập
                </a>
            </div>
        </div>
    </div>
</div>
